# Required (generate secrets with: `openssl rand -base64 32`)
DB_PASSWORD=changeme
AUTH_SECRET=changeme
AUTH_CLIENT_ID=changeme
AUTH_CLIENT_SECRET=changeme

# Optional overrides
# DB_NAME=runa
# DB_USER=postgres
# DB_PORT=5432
# API_PORT=4000
# APP_PORT=3000

# Platform service URLs (defaults work for local compose)
# AUTH_BASE_URL=http://localhost:3001
# BILLING_BASE_URL=http://localhost:4002
# AUTHZ_API_URL=http://warden-api:4001

# Scaling Notes:
# - Each API replica uses up to 20 database connections
# - For 3+ replicas, consider using PgBouncer for connection pooling
# - App service is stateless and scales horizontally without limits
