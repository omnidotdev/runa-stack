# Required (generate secrets with: `openssl rand -base64 32`)
DB_PASSWORD=changeme
AUTH_SECRET=changeme
AUTH_CLIENT_ID=changeme
AUTH_CLIENT_SECRET=changeme

# Optional overrides
# DB_NAME=runa
# DB_USER=postgres
# DB_PORT=5432
# API_PORT=4000
# APP_PORT=3000

# Optional platform service overrides
# AUTH_DB_NAME=auth
# BILLING_DB_NAME=billing
# WARDEN_API_PORT=4001
# GATEKEEPER_APP_PORT=3001
# AETHER_API_PORT=4002

# Optional URL overrides (defaults work for local development)
# BASE_URL=http://localhost:3000
# API_BASE_URL=http://localhost:4000
# AUTH_BASE_URL=http://localhost:3001
# BILLING_BASE_URL=http://localhost:4002
